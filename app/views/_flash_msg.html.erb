<% [:notice, :error, :alert].each do |type| %>
  <% if flash[type] %>
    <div class="alert ui-state-<%= type == :error ? 'error' : 'highlight' %> ui-corner-all <%= type.to_s %>"><%= flash[type] %><button class="close" data-dismiss="alert">×</button></div>
  <% end %>
<% end %>
