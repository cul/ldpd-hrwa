<% if paginate_params(@response).num_pages > 1 %>
    <div class="pagination span5">
      <%= paginate_rsolr_response @response, :inner_window => 2, :outer_window => 1, :theme => 'blacklight' %>
    </div>
<% end %>
