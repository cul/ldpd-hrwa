<%# not using bookmark/folder functions-%>
<%
#<li class="inline">
#< % link_to ("Selected Items" + ' (<span id="folder_number">' + (session[:folder_document_ids] ? session[:folder_document_ids].length : 0).to_s + '</span>)' ).html_safe, folder_index_path % >
#</li>
%>
<li class="inline">
<%= link_to "Search History", search_history_path %>
</li>
